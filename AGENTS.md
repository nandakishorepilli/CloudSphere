# CloudSphere contributor notes

- Keep the prototype beginner-friendly: FastAPI, SQLite, HTML, CSS, and vanilla JavaScript.
- Store local runtime database files under `data/`; do not commit them.
- Run the application from the project root with `python -m uvicorn app.main:app --reload`.
